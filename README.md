# ALX Listing App

The **ALX Listing App** is a project inspired by Airbnb.  
It aims to build a beautiful and functional listing page using **Next.js**, **TypeScript**, and **Tailwind CSS**.

## 🏗️ Project Structure

- `components/` – Reusable UI components (buttons, cards, headers, etc.)
- `interfaces/` – TypeScript interfaces for type safety
- `constants/` – Static data like categories, filters, etc.
- `public/assets/` – Images and SVGs used in the app

## 🚀 How to Run the Project Locally

1. Clone the repo  
   ```bash
   git clone https://github.com/rukohomini/alx-listing-app.git

